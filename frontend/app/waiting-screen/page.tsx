import WaitingScreenView from '@/views/public/waiting-screen/page';

export const metadata = {
  title: 'Waiting Area | MedFlow Clinic',
  description: 'Live patient queue tracking for waiting area displays.',
};

export default function WaitingScreenPage() {
  return <WaitingScreenView />;
}
