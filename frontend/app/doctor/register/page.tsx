import RegisterPatientView from '@/views/doctor/patients/register/page';

export const metadata = {
  title: 'Register Patient - MedFlow',
  description: 'Register a new patient into the system',
};

export default function RegisterPatientPage() {
  return <RegisterPatientView />;
}
