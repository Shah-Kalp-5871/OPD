apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: medflow-backend-hpa
  namespace: medflow
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: medflow-backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
