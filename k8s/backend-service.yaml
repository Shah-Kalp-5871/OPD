apiVersion: v1
kind: Service
metadata:
  name: medflow-backend
  namespace: medflow
  labels:
    app: medflow-backend
spec:
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: medflow-backend
  type: ClusterIP
